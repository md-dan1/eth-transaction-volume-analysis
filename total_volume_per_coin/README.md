This folder contains per-block Ethereum transaction volume broken down by coin:
- `todo.txt` ;-delimited CSV per period (`block_number;timestamp;<coin>…`, summed per block)
- `todo.png` the chart rendered from it

Columns are the top-20 coin set listed in /top_coins_by_tx_counts.txt.
Values are in USD.